# Short Scroll Fixture

This file is intentionally short.

Use it to verify:

- small documents do not get phantom scroll space
- wheel scrolling stops naturally at the top and bottom
- `j`, `k`, `gg`, and `G` behave sensibly
